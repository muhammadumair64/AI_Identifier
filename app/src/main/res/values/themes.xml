<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AIIdentifier" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/secondary</item>
        <item name="android:windowLightStatusBar">true</item>

        <item name="android:windowIsTranslucent">true</item>

        //******* set default color for all screens background*********//
        <item name="android:windowBackground">@color/secondary</item>
    </style>

    <style name="Theme.AIIdentifier" parent="Base.Theme.AIIdentifier" />
</resources>